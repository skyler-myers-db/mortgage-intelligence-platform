name: deploy-prod
on:
  workflow_dispatch:

jobs:
  prod-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: python tools/verify_scaffold.py
      - run: echo "Production deployment requires manual approval and Databricks workspace variables."
