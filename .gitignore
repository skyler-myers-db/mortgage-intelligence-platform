# secrets
.env
.env.*
!.env.example
secrets/
*.pem
*.key
*.token

# Python
.venv/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.pyc

# Node
node_modules/
frontend/node_modules/
frontend/dist/
coverage/
playwright-report/
test-results/
/dist/

# Databricks
.databricks/
.bundle/
# Resolved Genie space id written by tools/databricks/provision_genie_space.py
genie/space_id.txt
# Multi-catalog rendered SQL (build artifact produced by tools/render_sql.py
# from sql/**/*.sql for the target UC catalog). Canonical sources live in
# sql/** and stay committed; only the rendered copies under sql/_rendered
# are gitignored. See docs/multi-catalog-plan.md + docs/runbook-multi-catalog.md.
sql/_rendered/

# Claude local
.claude/
frontend/.claude/
.claude/settings.local.json
.claude/scheduled_tasks.lock
.mcp.json

# OS/editor
.DS_Store
.vscode/*.log

# Debug artifacts from Playwright MCP (screenshots generated during verification).
# Kept untracked so the root tree stays clean between slices.
.playwright-mcp/
/*.png
tsconfig.tsbuildinfo
frontend/tsconfig.tsbuildinfo

# Credential-kill-drill evidence logs -- one file per drill run, may embed
# partial dependency state + stack traces. Untracked so operator-laptop
# and CI evidence stays local; attach to release PRs manually.
tools/kill_drill/evidence/
