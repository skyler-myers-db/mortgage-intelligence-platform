# Hole-finder audit · 2026-04-23

Scope: places the first four route audits didn't go — error paths, dead
backend code, mismatched contracts, accessibility, secrets/infra. Twenty
findings below; one MINOR cluster fixed inline, the rest flagged.

Severity key
- **BLOCKER** — broken for every user on every deploy.
- **MAJOR** — works in happy path but fails on real-world inputs.
- **MINOR** — cosmetic / low-risk / easy to quietly fix.

| # | Category | File:line | What's wrong | Severity | Proposed fix |
|---|---------|-----------|--------------|----------|--------------|
| 1 | 1 Error paths | frontend/src/routes/home.tsx:94 | The KPI-load error banner reads "Couldn't load portfolio KPIs:" but has no **Retry** button. The user has to reload the whole route. DegradedBanner covers warehouse/lakebase, but the first-fetch 4xx case leaves the user stranded. | MAJOR | Add a `<button>Retry</button>` that re-triggers `api.portfolioPreview()` and resets `previewError`. Same pattern for `lead-queue.tsx:55`, `segment-intelligence.tsx:255`, `ask-genie.tsx:111`, `offer-orchestrator.tsx:159` — all five error blocks are retry-less. |
| 2 | 1 Error paths | frontend/src/components/mortgage/LeadTable.tsx:244 | Bulk-approve `Promise.all` per chunk: if the browser loses network mid-flight, approvals silently fall into the `fail` tally with **no retry prompt** beyond the next-click-to-retry convention. The toast summarises `ok/fail` but doesn't distinguish "network dropped" from "backend rejected". | MAJOR | Catch `ApiError.status === null` (network failure) separately and surface a distinct toast/alert ("Network dropped mid-approval. X done, Y queued for retry.") so the operator knows those approvals never reached the audit table. |
| 3 | 1 Error paths | backend/services/repositories/databricks_repo.py:371 | `_load_funnel` swallows **every exception** and returns `{}, {}`. A broken SQL warehouse, a malformed row, or a real bug all surface as "no trends data" in the UI — indistinguishable from empty. Resilience posture is fine, but the error is also not **logged**. | MINOR | Add `log.warning("funnel load failed: %s", exc)` before the return. |
| 4 | 1 Error paths | frontend/src/components/mortgage/AgentActivityLog.tsx:102 | Audit-feed fetch uses raw `fetch` (not `api.audit()`) so it bypasses the retry/backoff loop in `lib/api.ts`. On a 503 the feed shows "Audit feed is briefly unavailable" immediately instead of triggering the exponential-backoff re-fetch that `ApiError.retryable` enables. | MAJOR | Route through `api.ts` so 503+retryable responses hit the same backoff cadence as other reads. Same hit on `Topbar.tsx:44`, `DegradedBanner.tsx:43`, and `admin-config.tsx:114/127`. |
| 5 | 2 Test artifacts | backend/services/repositories/__init__.py + factory.py | **The entire pytest suite fails to collect on this branch.** `backend/services/repositories/__init__.py` exports `get_geo_repository` and `GeoRepository`, but neither exists in `factory.py` / `protocols.py`. Every test that imports `backend.main` (via conftest.py) hits `ImportError: cannot import name 'get_geo_repository'` before a single test runs. `backend/api/geo.py` is also NOT in `main.py`'s router include list and the test `tests/unit/test_geo_state_rollups.py` expects the URL to work. Half-deployed geo feature breaks both prod and CI. | BLOCKER | Add `GeoRepository` to `protocols.py`, `get_geo_repository` to `factory.py`, a `state_rollups()` method in the repo, and `geo.router` to `main.py`'s include list — OR revert the `__init__.py` exports + delete `backend/api/geo.py` + `tests/unit/test_geo_state_rollups.py` until the repo exists. Until then, `pytest` won't collect. |
| 6 | 2 Test artifacts | backend/api/portfolio.py:32 + :37 | `/api/portfolio/create` and `/api/portfolio/{portfolio_id}` are registered but **no frontend caller exists** (grep: zero hits). `/api/portfolio/{id}` has **no `response_model`** so fields can silently drop. | MINOR | Either wire the Portfolio Builder "Run build" CTA to `/api/portfolio/create` (it currently only re-previews) or remove both endpoints. |
| 7 | 2 Test artifacts | backend/api/outreach.py:60 | `/api/outreach/draft` is called from `offer-orchestrator.tsx` but **emits a `DRAFT_OUTREACH` audit row as a side effect on every render/id change**. Because the route refetches on mount, navigating between borrowers floods Lakebase with draft-view audit events that represent "an operator rendered the Offer page", not "an operator asked for a draft". | MAJOR | Either rename the event type (`VIEW_DRAFT` vs `DRAFT_OUTREACH`) or gate emission on an explicit user action (Regenerate button). |
| 8 | 2 Test artifacts | backend/api/admin.py:7,15 | `_RULES` is a **module-level dict**. PUT /api/admin/rules mutates it in-process; any restart or workspace rotation loses the change. There's no persistence, no audit, and no multi-worker safety. | MAJOR | Either remove PUT (read-only surface) or back it with Lakebase + emit an `ADMIN_RULES_EDIT` audit row. |
| 9 | 3 Copy lies | frontend/src/routes/admin-config.tsx:55 | `RULES_EDITED_AT = '2026-03-15'` is hardcoded. The surface renders "Edited · Active — 2026-03-15" regardless of what the backend returns, so a stale/broken `/api/admin/rules` endpoint still shows a plausible recent date. | MINOR | Read `edited_at` from the backend (add to `GET /api/admin/rules` response) or drop the field. |
| 10 | 3 Copy lies | frontend/src/routes/admin-config.tsx:150 | When `/api/admin/rules` fails, `rulesError` is set — but the UI chip still displays the hardcoded fallback `'rules.itm_v3'`. Looks healthy when it isn't. | MINOR | Set chip variant to `warning` and show "rules unavailable" when `rulesError !== null`. |
| 11 | 3 Copy lies | frontend/src/components/mortgage/USChoroplethMap.tsx:101 (STATE_FACTS) | The geography map's per-state counts are **50 hardcoded literals** ("IL 1860, CA 900, FL 760 ..."). The live rollup wiring is half-done (see finding #5) — until then, the hero map shows synthetic counts the Home talk track claims are "real multi-state share". | MAJOR | Complete the geo endpoint wiring (finding #5) or re-label the map chip as "synthetic until state rollup ships". |
| 12 | 3 Copy lies | frontend/src/components/mortgage/AgentActivityLog.tsx:202 / Rail.tsx:22 / admin-config.tsx:256 | Multiple chips/labels say "live" ("Live dependency telemetry", "Top-of-Funnel · live today", "6 of 8 live") when the underlying data refreshes nightly (see `Delta Share · nightly` on `admin-config.tsx:64`). Conflates "endpoint is up right now" with "data is live-streamed". | MINOR | Use "up/ok" for dep state, reserve "live" for real-time surfaces only. |
| 13 | 4 Contract drops | backend/schemas/lead.py:24 | `LeadSummary.display_name: str` is **required** on the wire — but the frontend renders it exactly nowhere (LeadTable renders `borrower_id`, `clip`, city, etc.). Payload bloat + an enforced PII-ish field nobody reads. | MINOR | Drop from `LeadSummary` and keep only on `Borrower360` where the dossier header needs it. |
| 14 | 4 Contract drops | backend/api/admin.py:22 | `/api/admin/settings` returns `{app_env, lender_name, catalog, gold_schema, lakebase_schema, warehouse_id}` with **no `response_model`** — dict serialised as-is. A future settings addition will leak to the wire without review. Also never called by the frontend. | MINOR | Delete the endpoint or give it a Pydantic schema + a caller. |
| 15 | 4 Contract drops | backend/api/borrowers.py:80 | `/api/borrowers/{id}/evidence` is defined but the frontend never calls it (it reads `evidence_events` from the `/api/borrowers/{id}` payload instead). Dead router. | MINOR | Remove. |
| 16 | 5 A11y | frontend/src/components/mortgage/USChoroplethMap.tsx:471 / :586 / :797 | State + county + ZIP `<path>` elements have `role="button"` + `tabIndex={0}` but **no `onKeyDown` handler** — keyboard users can focus the region but can't activate (Enter/Space do nothing). Map drill-down is mouse-only. | MAJOR | Add `onKeyDown={(e) => e.key === 'Enter' || e.key === ' ' ? onClickLogic() : undefined}` on every clickable `<path>`. |
| 17 | 5 A11y | frontend/src/components/mortgage/EvidenceDrawer.tsx:25 | `<aside role="dialog">` is a modal surface with **no ESC-to-close handler, no focus trap, no focus return on close**. Opens a scrim, but keyboard users can tab into the page behind it. | MAJOR | Add `useEffect` that listens for `Escape` while `drawer !== null`, plus a focus trap or `inert` on siblings, and restore focus to the opener on close. |
| 18 | 5 A11y | frontend/src/components/mortgage/USChoroplethMap.tsx:862 + CSS | SVG `<animate>` elements (IllinoisBeacon pulse + Cook County beacon) run indefinitely and **don't respect `prefers-reduced-motion`** — CSS reduced-motion guards don't apply to SVG SMIL. A reduced-motion user sees perpetual pulsing. | MINOR | Wrap the `<circle>` + `<animate>` in a reduced-motion check via a `useMatchMedia('(prefers-reduced-motion: reduce)')` hook; render the static circle only when set. |
| 19 | 6 Security | backend/config/settings.py:112 | `default_actor: str = "skyler@entrada.ai"` is hardcoded. When `X-Forwarded-Email` is absent (local dev, misconfigured prod), every audit row is attributed to Skyler's email across all customer deploys. | MAJOR | Default to a sentinel like `unknown@local` or require the header in prod via a settings validator. |
| 20 | 6 Security | backend/api/outreach.py:149 | `draft_body` is written into the `action_audit.payload_json` column on approve — but the body may contain the borrower's city/state and LO-written text. PII posture says synthesised only; a free-text field makes the audit surface PII-adjacent if a LO types a name into the textarea. | MAJOR | Either redact/strip on the audit write, or scan the LO-edited draft for obvious PII before persisting; add a test. |
| 21 | 7 Perf | frontend/src/components/layout/Topbar.tsx:55 + AgentActivityLog.tsx:150 + DegradedBanner.tsx | **Three independent pollers** all hit `/api/health` every ~30s — same payload, no shared cache. First-render also fires three extra identical fetches. | MINOR | Lift health polling into a single context provider; components read from context. |
| 22 | 7 Perf | frontend/src/components/mortgage/USChoroplethMap.tsx:548 | `useMemo(totalCount, [level, countyStateId, countiesByState])` — was missing `liveStateFacts` in the dep array, so `totalCount` staled when state rollups hydrated. Fixed inline (see gates). | MINOR | **Fixed inline**. |
| 23 | 7 Perf | frontend/src/routes/offer-orchestrator.tsx:87 | Every mount (+ every `id` change) fires `Promise.all([api.borrower(id), api.recommendOffer(id)])` + `api.draftOutreach(id)` in parallel **without a cache**. Navigating back/forward refetches the same borrower. The resilience layer's 30s TTL cache is server-side for the preview, not the dossier. | MINOR | Add client-side SWR/stale-while-revalidate for borrower/recommend/draft with `borrower_id` key. |
| 24 | 8 Infra | databricks.yml:10,27,45,61 | `workspace.host = https://dbc-3aa503a9-4fa8.cloud.databricks.com` is **hardcoded in dev + prod + ci + staging** targets — all four point at Entrada's workspace. A customer deploy would silently land on Entrada's workspace unless the operator edits YAML per the "CUSTOMER-SE CHECKLIST" comment. Zero-click deploy claim (CLAUDE.md §What to optimize) doesn't hold for a non-Entrada customer. | MAJOR | Parametrise `workspace.host` through `variables:` so a customer deploy passes `--var host=https://...` instead of editing the file, or require `DATABRICKS_HOST` from `.env.local`. |
| 25 | 8 Infra | backend/main.py (router include list) | The `include_router` list at main.py omits `backend/api/geo.py` (see finding #5). Nightly tests attempt to call an unregistered endpoint; CI would flag this, and currently does — the skipped `tests/unit/test_geo_state_rollups.py` suite is marked FAILED in the 2026-04-22 nightly log. | BLOCKER | See #5 — either register or delete. |

## Fixed inline

- `frontend/src/mocks/fixtureData.ts` — `mockPortfolio` was missing required `top_tier_opportunities` / `offers_recommended` fields → `tsc -b` was failing. Added synthesized counts. (NOTE: the file was re-synced during this audit pass; the types.ts / fixtureData.ts now agree without those keys, so the delta may have landed already.)
- `frontend/src/types.ts` — added missing `StateRollup` / `StateRollupResponse` exports so `api.ts` import resolves (was breaking `npm run build`).
- `frontend/src/components/mortgage/USChoroplethMap.tsx:548` — added `liveStateFacts` to `useMemo` deps (stale-total bug under live-rollup data).

## Gates run

```
npm --prefix frontend run lint            ✓ 0 warnings, --max-warnings 0
npm --prefix frontend run build           ✓ tsc -b && vite build
.venv/bin/ruff check backend tests tools  ✓ All checks passed
.venv/bin/pytest tests/unit -q            ✗ ImportError at collection
                                            — finding #5 blocks every
                                              test from running.
```

No commit. The pytest failure IS one of the findings — leaving the gate
red so it shows up in the report is load-bearing evidence for the
master agent.

## What I searched for but did not find

- No `console.log/debug/warn/error` statements in production frontend code (all clean).
- No `fixtureData`/`demo-borrowers` mock import by any production route or component. Mocks are test-only per the CLAUDE.md contract.
- No `process.env.NODE_ENV`/`import.meta.env` dev-only UI branches.
- No obvious secret leaks (`sk-…`, `Bearer …`, `AWS_SECRET`) in source.
- No FIXME/XXX/HACK comments pointing to known-broken runtime behavior; the TODOs found were tracked enhancement notes.
- No `<div onClick>` patterns standing in for `<button>` — SegmentCard / Topbar / Console / FilterSelect are all real buttons. The table rows using `<tr onClick>` are semantically fine for expandable rows.
- No double-polling N+1 beyond the `/api/health` triplet already flagged.
- No `schedule_quartz_cron_expression` on a continuous compute resource — Lakeflow pipelines are `continuous: false`; jobs respect dev-mode auto-pause.
