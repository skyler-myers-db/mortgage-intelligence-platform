> **Internal implementation artifact. Not approved for public release.**

# Validation: Load Baseline

The canonical load-baseline operator document is
[`docs/load-baseline.md`](../load-baseline.md). It contains the validated
read/write p95 tables, active deployment evidence, commands, and baseline
refresh procedure.

This file intentionally stays as a pointer so operators have one source of
truth for sustained-load expectations.
