[project]
name = "mortgage-intelligence-platform"
version = "0.1.0"
description = "Mortgage Intelligence Platform backend"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
testpaths = ["tests"]
