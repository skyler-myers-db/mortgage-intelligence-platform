resources:
  dashboards:
    executive_dashboard:
      display_name: MIP Module 0 Executive Dashboard
      file_path: ../dashboards/executive_dashboard.lvdash.json
      warehouse_id: ${var.sql_warehouse_id}
    segment_dashboard:
      display_name: MIP Segment Intelligence Dashboard
      file_path: ../dashboards/segment_dashboard.lvdash.json
      warehouse_id: ${var.sql_warehouse_id}
