"""Placeholder superseded by ``tests/integration/test_genie_live.py``.

Slice 7 renamed the live-creds-gated Genie integration test to
``test_genie_live`` to avoid a module-name collision with
``tests/unit/test_genie_client.py`` (pytest refuses to import two test
modules with identical basenames from different directories). This
file intentionally contains no tests; see ``test_genie_live`` for the
live Genie Conversation API coverage.
"""
